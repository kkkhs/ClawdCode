/**
 * 配置模块导出
 */

export { ConfigManager, configManager } from './ConfigManager.js';
export { ConfigSchema, ModelConfigSchema, DEFAULT_CONFIG } from './types.js';
export type { Config, ModelConfig, UIConfig } from './types.js';
